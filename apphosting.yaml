# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  sessionAffinity: true
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

backend:
  service_id: ceosw-04-trackone